/** Shared between app/api/cogniterra/oauth/start and .../callback. */
export const STATE_COOKIE = "cogniterra_oauth_state";
/** Where to redirect back to after the flow completes - defaults to /instructor. */
export const RETURN_TO_COOKIE = "cogniterra_oauth_return_to";
export const DEFAULT_RETURN_TO = "/instructor";
